interface:
  display_name: "Grill With Docs"
  short_description: "User-started decision interview that only proposes glossary and ADR patches"
  default_prompt: "Resolve decisions first, then propose separate CONTEXT.md and ADR patches. Do not modify formal sources until the user approves."
policy:
  allow_implicit_invocation: false
