# Standalone Session Workflow — Engineering Closure

**日期：** 2026-08-19
**狀態：** ENGINEERING COMPLETE
**分支：** `feature/standalone-session-workflow-20260819`
**HEAD：** `6d23e73`（與 `origin/master` 相同；本功能尚未 commit）

## 1. 結論

Standalone Session Workflow 的 Phase 0–5 已完成並通過最終 regression。

目前架構：

```text
Session Workflow Core
├─ durable task/state machine
├─ JSONL ledger + hash chain
├─ lock
├─ revision CAS
├─ idempotency
├─ scope / project isolation
├─ credential persistence gate
├─ create/list/update
└─ generic extension mutation seam

Standalone runtime
├─ StandaloneProjectResolver
├─ official MCP TypeScript SDK v2
├─ workflow_create
├─ workflow_list
├─ workflow_update
├─ local loopback server
├─ OAuth owner authorization server
├─ OAuth protected MCP resource server
└─ Windows portable / Dev Tunnel lifecycle

DevSpace optional integration
├─ DevSpaceProjectResolver
├─ local Agent adapter
├─ model / reasoning policy
├─ workflow_run
└─ workflow_sync
```

Standalone Core 沒有 DevSpace Agent runtime dependency；DevSpace run/sync 透過 generic extension seam 寫回同一個 Core ledger，沒有第二套 workflow state machine。

## 2. Acceptance Criteria 對照

### AC-01 / objective：既有 DevSpace workflow 相容

PASS。

最終 DevSpace workflow tests：

```text
22 passed
0 failed
```

DevSpace MCP 仍提供五個 tools：

```text
workflow_create
workflow_list
workflow_update
workflow_run
workflow_sync
```

### AC-02：Core 不依賴 DevSpace workspace registry / Agent runtime / PixiuCore vault-memory

PASS。

Core source boundary scan 對以下 pattern 無 match：

```text
DEVSPACE_
DevSpace
workflow_run
workflow_sync
Agent adapter
agentAdapter
runTask
syncTask
```

Standalone MCP / Standalone resolver 也無 `DevSpace`、`PixiuCore`、vault/memory runtime reference。

### AC-03：Standalone MCP 不需要 DevSpace

PASS。

Standalone tests：

```text
12 passed
0 failed
0 skipped
```

包含 official MCP Client 連線與 tool call。

### AC-04：create/list/update/handoff/acknowledge

PASS。

兩個獨立官方 MCP Client 已完成：

```text
Session A
→ workflow_create
→ claim
→ handoff

Session B
→ workflow_list
→ 讀取 handoff context
→ acknowledge
→ currentOwner = Session B actor
```

### AC-05：DevSpace run/sync optional adapter

PASS。

Core 直接 import 時：

```text
runTask === undefined
syncTask === undefined
agentAdapter === undefined
```

DevSpace compatibility layer 再掛回 run/sync，既有 behavior tests 全通過。

### AC-06：每個 Phase durable handoff

PASS。

Durable task：

```text
tsk_939d8c453fab
```

Phase 0、1、2、3A/3B、4、5 都有 handoff checkpoint。

### AC-07：ledger integrity / CAS / idempotency / isolation / credential / review

PASS。

最終 regression 覆蓋：

- hash tamper fail closed。
- truncated ledger fail closed。
- V1 replay。
- V1→V2 migration 不改舊 hash。
- same revision concurrent mutation 只一個成功。
- retry idempotency 不啟動第二個 Agent。
- same-project / cross-project visibility isolation。
- credential-like persistent input 拒絕。
- Agent runtime output credential redaction。
- fixed revision independent review gate。
- Agent start finalization 保留 concurrent handoff revision。

## 3. Official MCP / OAuth

### MCP runtime

Exact pin：

```text
@modelcontextprotocol/server 2.0.0
@modelcontextprotocol/node   2.0.0
@modelcontextprotocol/client 2.0.0  # test only
zod 4.4.3
```

Standalone endpoint 使用官方：

- `McpServer`。
- `createMcpHandler()`。
- `toNodeHandler()`。
- official MCP Client integration tests。

Legacy initialize 與 2026-07-28 discovery negotiation 都有 wire test。

### OAuth owner mode

已實作：

- RFC 9728 path-aware Protected Resource Metadata。
- Dynamic Client Registration public client。
- Authorization Code。
- PKCE S256。
- owner password approval。
- bearer token verification before MCP handler。
- official `WWW-Authenticate` bearer challenge。
- access-token expiry。
- refresh-token rotation。
- `offline_access` scope compatibility。
- restart persistence。

Persistent OAuth state 不保存：

- raw owner password。
- raw access token。
- raw refresh token。

只保存 client registrations 與 token hashes。

## 4. Portable release

Release：

```text
C:\PixiuCore\artifacts\session-workflow-portable\SessionWorkflowOneClick-0.1.0.zip
```

大小：

```text
3,486,087 bytes
```

SHA-256：

```text
6081a74eb8dec8be4810a1dfe7a79af1c4f1b9cd07a9ec77366eb9baf761c919
```

Release 內容只包含：

- Core。
- Standalone resolver。
- MCP runtime。
- production dependencies。
- Windows portable lifecycle。

Release 不包含：

- DevSpace resolver/runtime。
- PixiuCore vault/memory。
- development tests。
- test-only MCP client dependency。
- `owner-secret.dpapi`。
- `settings.json` runtime state。
- `runtime.json` runtime state。
- existing workflow state。

ZIP 解壓到 `%TEMP%` 後使用 bundle 自己的 dependencies 啟動 server：

```text
PASS
```

Smoke 同時掃描 standalone runtime source，禁止 DevSpace / PixiuCore runtime reference。

## 5. Windows portable lifecycle

提供：

```text
01-INSTALL-AND-START.cmd
02-START.cmd
03-STOP.cmd
04-STATUS.cmd
05-COPY-MCP-URL.cmd
06-COPY-OWNER-PASSWORD.cmd
07-REPAIR.cmd
08-REMOVE.cmd
```

安全設計：

- Session Workflow process 只 bind loopback。
- HTTPS Dev Tunnel 作 remote transport。
- `/mcp` 仍要求 OAuth Bearer token。
- Windows DPAPI CurrentUser 保護 owner password。
- PID identity guard，避免誤殺其他 process。
- remove 先刪 tunnel；外部 tunnel delete 失敗時保留 local identity/state。
- dependency repair 使用 exact lockfile production install。

Windows PowerShell 5.1 compatibility 已針對 RNG、ProtectedData assembly、StrictMode optional property、builder parsing 修復並測試。

## 6. 最終測試矩陣

### Standalone package

```text
12 passed
0 failed
0 skipped
```

### DevSpace workflow

```text
22 passed
0 failed
```

### Standalone portable

```text
12 passed
0 failed
```

### DevSpace OneClick

```text
110 passed
0 failed
```

### Standalone ZIP smoke

```text
PASS
```

### Production dependency audit

```text
0 vulnerabilities
```

### Git integrity

```text
git diff --check = PASS
```

唯一訊息為 Windows Git 未來可能將 `DevSpace.WorkflowStore.mjs` LF 轉為 CRLF；不是 whitespace error。

## 7. Working tree 收斂

現在 `git status` 只顯示本功能相關 source：

Tracked modifications：

- `.gitignore`
- `scripts/devspace-portable/DevSpace.OneClick.Subagents.psm1`
- `scripts/devspace-portable/DevSpace.WorkflowStore.mjs`
- `scripts/devspace-portable/devspace-oneclick.ps1`
- `scripts/devspace-portable/tests/run-tests.ps1`
- `scripts/devspace-portable/tests/workflow-store.test.mjs`

Untracked source 都位於：

- `packages/session-workflow/`
- `scripts/session-workflow-portable/`
- `specs/active/02-standalone-session-workflow/`

已加入 ignore：

- `packages/session-workflow/node_modules/`
- `artifacts/session-workflow-portable/`

因此 dependency tree 與 release build output 不會誤進 Git。

舊的 safety stash 保持不動；沒有執行 pop/reset/stash。

## 8. 外部環境驗證邊界

本次刻意沒有為測試執行下列外部 account mutation：

- 建立真實 Microsoft Dev Tunnel。
- 刪除真實 Microsoft Dev Tunnel。
- 在使用者 ChatGPT workspace 建立真實 custom MCP app / connector。

已完成替代驗證：

- Dev Tunnel CLI command surface read-only 驗證。
- local OAuth HTTP E2E。
- official MCP Client Bearer E2E。
- public URL / metadata / security contract tests。
- repository-independent Windows ZIP smoke。

因此 engineering implementation 完成；第一次真實朋友電腦 + Microsoft Dev Tunnel + ChatGPT Web 連線屬 deployment acceptance，不應以自動測試擅自修改使用者外部帳號。

## 9. Phase 6 Repo Extraction

狀態：

```text
COMPLETE
```

已完成：

- 建立獨立 GitHub repository：`pop15106/session-workflow`。
- Repository visibility：Private。
- Standalone source、tests、portable installer、README 與獨立工程文件已搬入新 repo。
- 新 repo initial commit：`8649345 feat: publish standalone session workflow`。
- 新 repo `main` 已 push 到 `origin/main`。
- 新 repo 直接驗證：package 12/12、portable 12/12、ZIP independent smoke PASS、npm audit 0 vulnerabilities。
- PixiuCore 改成透過 `external/session-workflow` Git submodule 消費新 repo。
- DevSpace-specific project resolver 留在 PixiuCore integration layer，不污染 Standalone repo。
- PixiuCore 原本重複的 Standalone source / portable source 已移除。

## 10. Git 操作

Standalone repo 已完成：

```text
git add
git commit
git push
```

PixiuCore feature branch 會在 extraction integration regression 通過後完成 commit / push。

安全 stash 保持不動；沒有執行：

```text
git reset
git stash
git stash pop
```

## 11. Closure

Standalone repository extraction 已完成。

剩餘工作只包含 PixiuCore consumer integration 的最後 regression、stage、commit 與 push。
