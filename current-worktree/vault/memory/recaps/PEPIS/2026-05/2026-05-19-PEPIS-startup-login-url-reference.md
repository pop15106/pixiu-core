---
type: session-recap
date: 2026-05-19
project: PEPIS
system: PEPIS
repo: pepis_ap
topic: pepis-startup-login-url-reference
status: done
tags: [recap, pepis, ccps, startup, login, announcement, vault-reference]
source_paths:
  - C:/Users/7010/Desktop/gravityTest/pixiu-core/vault/projects/PEPIS/startup-commands.md
  - C:/Users/7010/Desktop/gravityTest/pixiu-core/vault/projects/PEPIS/index.md
  - C:/Users/7010/Desktop/Project/pepis_ap/view/CCPS/src/router/index.js
  - C:/Users/7010/Desktop/Project/pepis_ap/view/CCPS/src/views/Login.vue
  - C:/Users/7010/Desktop/Project/pepis_ap/view/CCPS/vue.config.js
  - C:/Users/7010/Desktop/Project/pepis_ap/pom.xml
summary: 回寫 PEPIS / CCPS 本機啟動指令到 vault，並確認新登入頁 URL、force query 與公告位置切換方式。
---

# 2026-05-19 PEPIS 啟動指令與登入頁 URL Reference

## Trigger And Context

- 使用者要保存 `pepis_ap` 前後端啟動指令，避免之後重查。
- 使用者補充前端主線是 `view/CCPS`，並記得用 `vue ui` 啟動。
- 後續又詢問先前新登入頁、忘記密碼功能與公告位置調整後，應如何進入登入頁。

## Conclusion

- 已新增長期 reference：`vault/projects/PEPIS/startup-commands.md`。
- 已在 `vault/projects/PEPIS/index.md` 補上 `[[startup-commands|啟動指令]]` 入口。
- 登入頁 route 是 Vue Router 的 `/login`，目前 router 未設定 history mode，因此本機 URL 使用 hash route。
- 建議測試登入頁網址：

```text
http://localhost:8080/APEPIS/CCPS/#/login?force=1&announcementPosition=right
```

- 若走後端 Tomcat build 產物，可用：

```text
http://localhost:8233/APEPIS/CCPS/#/login?force=1&announcementPosition=right
```

## Evidence Or Flow

- `view/CCPS/vue.config.js`
  - `publicPath: "/APEPIS/CCPS"`
  - dev server proxy target: `http://localhost:8233`
- `pom.xml`
  - `tomcat7-maven-plugin`
  - context path: `/APEPIS`
  - port: `8233`
- `view/CCPS/src/router/index.js`
  - `path: '/login'`
  - `to.query.force === '1'` 時，即使已登入也保留在登入頁，方便測試登入表單、驗證碼與忘記密碼。
- `view/CCPS/src/views/Login.vue`
  - `announcementPosition` query 支援 `left|right|top|bottom`
  - 預設值是 `right`
  - `created()` 呼叫 `loadAnnouncements()`
  - 公告 API：`/rest/auth/announcements`

## Changes Made

- 新增 `vault/projects/PEPIS/startup-commands.md`，內容包含：
  - 後端：`mvn compile tomcat7:run -e`
  - 前端主線：`cd ...\view\CCPS` 後執行 `vue ui`
  - 前端 CLI 等效：`npm run serve`
  - 首次安裝：`npm install`
  - 參考 URL 與 Node v16 備註
  - `view/ccps_re` 僅標為備用 React 專案，不列為主線
- 更新 `vault/projects/PEPIS/index.md`，新增啟動指令入口。

## Verification

- 以 UTF-8 讀取 `startup-commands.md`，確認 frontmatter 與中文內容正常。
- `Select-String` 確認 `vault/projects/PEPIS/index.md` 已有 `startup-commands` 連結。
- `Select-String` 確認 repo 現況：
  - `/login` route 存在
  - `force=1` query 存在
  - `announcementPosition` query 存在，預設 `right`
  - `publicPath` 是 `/APEPIS/CCPS`
  - Tomcat port 是 `8233`

## Notes

- second-brain 命中兩份相關 recap：
  - `2026-05-13-pepis-login-forgot-password-captcha.md`
  - `2026-05-14-102604-pepis-login-announcement-ui-recap.md`
- 2026-05-14 公告 UI recap 記錄：公告區塊已改成 table-like 清單，預設公告位置改成右側，query 切換仍保留。
- 注意：2026-05-13 recap 的追加更正提到忘記密碼表單不需要 captcha，但本次直接讀目前 repo 的 `Login.vue` 時仍看到 `forgotCaptcha` 與 `forgotForm.captchaCode`。若後續測忘記密碼流程與預期不一致，應以目前 repo code 再做一次實測與 tracing。

## Next Steps

- [ ] 若要實測新登入頁，先啟動後端 `mvn compile tomcat7:run -e`，再用 Vue UI 啟動 `view/CCPS` 的 `serve`。
- [ ] 用 `force=1` 進入登入頁，測登入 captcha、忘記密碼表單、公告 loading/empty/error/展開行為。

---

*Generated by Codex from the active PEPIS session.*
